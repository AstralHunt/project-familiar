/** @type {import('next').NextConfig} */
const nextConfig = {
  devIndicators: {
    buildActivity: false,
  },
  images: {
    unoptimized: true,
  },
  // output: 'export',


};

export default nextConfig;
